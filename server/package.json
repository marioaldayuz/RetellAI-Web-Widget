{
  "name": "retell-proxy-server",
  "version": "2.0.0",
  "description": "Universal proxy server for RetellAI widget - supports any domain deployment",
  "main": "server.js",
  "keywords": ["retell", "proxy", "cors", "universal", "widget"],
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Server build complete'",
    "setup": "npm install && npm run setup:env",
    "setup:env": "node setup-env.js",
    "test": "node test-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
